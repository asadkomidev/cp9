import BackgroundGrid from "@/components/bg-grid";
import Tools from "./tools";

type Props = {};

const ToolsLinks = (props: Props) => {
  return (
    <main className="flex h-screen items-center justify-center">
      <Tools />
    </main>
  );
};

export default ToolsLinks;
